import { defineConfig } from 'windicss/helpers'

export default defineConfig({
    theme: {
        flex: {
            1: '1 1 0%',
            auto: '1 1 auto',
            initial: '0 1 auto',
            fill: '1 0 100%',
            inherit: 'inherit',
            none: 'none',
            2: '2 2 0%',
        },
    }
})
